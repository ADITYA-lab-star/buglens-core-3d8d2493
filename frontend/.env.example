# BugLens Frontend — Environment Variables
#
# Copy this file to .env.local and fill in the values.
# .env.local is git-ignored and never committed.
#
# In Netlify: set VITE_API_URL in Site → Environment Variables.
# Vite replaces import.meta.env.VITE_API_URL at build time.

# URL of the deployed FastAPI backend (no trailing slash)
# Example: https://buglens-api.onrender.com/api/v1
VITE_API_URL=http://localhost:8000/api/v1
