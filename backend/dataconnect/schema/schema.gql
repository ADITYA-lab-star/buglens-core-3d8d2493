# backend/dataconnect/schema.gql

# 1. User Profile Table
type User @table {
  id: String!
  email: String!
}

# 2. Code Reviews Table 
type Review @table {
  id: String!
  userId: String!
  user: User!
  repoName: String!
  language: String!
  status: String!
  metadataJson: String! 
}