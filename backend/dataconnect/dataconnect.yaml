specVersion: "v1alpha"
serviceId: "glens-production-service"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "glens-production-service" # Tells Firebase to inject the tables straight into your live trial database instance
connectorDirs:
  - "./example"